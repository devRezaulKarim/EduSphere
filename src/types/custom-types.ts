export type Feature = {
  id: number;
  icon: string;
  title: string;
  subtitle: string;
};
export type Route = {
  label: string;
  href: string;
};
export type Step = {
  id: number;
  icon: string;
  title: string;
  description: string;
};
